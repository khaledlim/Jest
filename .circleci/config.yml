version: 2

# Define the jobs we want to run for this project
jobs:
  build:
    docker:
      - image: circleci/node:6.13.4
    steps:
      - checkout
      - run: npm install
      - run: npm test
